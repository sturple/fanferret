{% extends 'FanFerretQuestionBundle:Survey:base.html.twig' %}

{% block content %}
<div class="container">
	<div class="content-wrapper">
		{% if survey.params.logo is defined %}
		<img src="{{survey.params.logo|escape('html_attr')}}" alt="{{survey.params.title}}">
		{% endif %}
		{% if survey.params.title is defined %}
		<h1>{{survey.params.title}}</h1>
		{% endif %}
		<p>Thank you for completing our online Guest Comment Card.
		We truly appreciated it and we will share any comments you've provided with our staff.</p>

		<p>On behalf of all of us at {{survey.property.name}},
		thank you for choosing to stay with us during your time and we hope to see you again very soon.</p>

		<p>Warmest Regards,</p>
		<p>
    	{{survey.params.notifications.name | default('Fanferret Team')}}<br />
		{% if survey.params.notifications.jobtitle %}{{survey.params.notifications.jobtitle}}<br />{% endif%}
		{% if survey.params.notifications.email %}<a href="mailto:{{survey.params.notifications.email}}" target="_blank">{{survey.params.notifications.email}}</a><br /><br />{% endif%}
		{% if survey.params.notifications.phone %}{{survey.params.notifications.phone}}{% endif%}
		</p>

	</div>
</div>
{% endblock %}